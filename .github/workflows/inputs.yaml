name: Inputsparameter
on:
  workflow_dispatch:
    inputs:
      environment:
        description: choose the env to depoloy
        required: true
        default: dev
jobs:
  inputs-demo:
    runs-on: ubuntu-latest
    steps:
      - name: inputsparmeter
        run: echo "depolying the application to ${{inputs.environment}}"
