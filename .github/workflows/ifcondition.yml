name: if condition
on:
  push:
    branches:
      - dev
      - main
jobs:
  Job1:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        if: github.ref == 'refs/heads/dev'
        run: echo "Saikiran is tiggered the file" 
      - name: step2
        run: echo "saikiran is a waste fellow"
    
    
      
